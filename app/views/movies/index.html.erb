<main role="main">

   <section class="jumbotron text-center">
     <div class="container">
       <h1 class="jumbotron-heading">왓챠! 영화 평점 서비스</h1>
       <p class="lead text-muted">엄청나게 많은 영화와 평점</p>
       <!-- <p>
         <a href="#" class="btn btn-primary">Main call to action</a>
         <a href="#" class="btn btn-secondary">Secondary action</a>
       </p> -->
     </div>
   </section>

   <div class="album text-muted">
     <div class="container">
       <div class="row">
         <% @movies.each do |movie| %>
           <div class="card">
             <%= link_to movie_path(movie) do %>
             <img src="<%= movie.image_url %>" alt="Card image cap">
             <p class="card-text"><%= truncate movie.desc, length: 50, omission: "..." %></p>
             <% end %>
           </div>
         <% end %>
       </div>
     </div>
   </div>
 </main>
