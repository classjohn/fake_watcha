<!-- Always shows a header, even in smaller screens. -->
<div class="mdl-layout mdl-js-layout mdl-layout--fixed-header">
<header class="mdl-layout__header">
  <div class="mdl-layout__header-row">
    <!-- Title -->
    <span class="mdl-layout-title">왓챠</span>
    <!-- Add spacer, to align navigation to the right -->
    <div class="mdl-layout-spacer"></div>
    <!-- Navigation. We hide it in small screens. -->
    <nav class="mdl-navigation mdl-layout--large-screen-only">
      <a class="mdl-navigation__link" href=""><%= current_user.email if current_user %>님 환영합니다.</a>
      <%= link_to '로그인', new_user_session_path, class: "mdl-navigation__link" %>
      <%= link_to '로그아웃', destroy_user_session_path, method: :delete, class: "mdl-navigation__link"%>
      <%= link_to '관리자', admin_users_path, class: "mdl-navigation__link" %>
    </nav>
  </div>
</header>
<div class="mdl-layout__drawer">
  <span class="mdl-layout-title">Title</span>
  <nav class="mdl-navigation">
    <a class="mdl-navigation__link" href=""><%= current_user.email if current_user %>님 환영합니다.</a>
    <%= link_to '로그인', new_user_session_path, class: "mdl-navigation__link" %>
    <%= link_to '로그아웃', destroy_user_session_path, method: :delete, class: "mdl-navigation__link"%>
    <a class="mdl-navigation__link" href="">Link</a>
  </nav>
</div>
<main class="mdl-layout__content">
  <div class="page-content"><!-- Your content goes here --></div>
</main>
</div>
